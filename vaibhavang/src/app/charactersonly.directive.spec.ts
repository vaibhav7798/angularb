import { CharactersonlyDirective } from './charactersonly.directive';

describe('CharactersonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharactersonlyDirective();
    expect(directive).toBeTruthy();
  });
});
